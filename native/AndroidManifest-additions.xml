<!--
  Pegar DENTRO de <application> en android/app/src/main/AndroidManifest.xml
  (generado por `cap add android`). El service debe declararse con el permiso
  BIND_NOTIFICATION_LISTENER_SERVICE y el intent-filter del listener.
-->
<service
    android:name="com.vitali.monetary.MonetaryNotificationListenerService"
    android:label="Monetary — lector de notificaciones"
    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
    android:exported="false">
    <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
    </intent-filter>
</service>

<!-- Permiso de internet ya viene por defecto en Capacitor. Nada más se necesita:
     el "Acceso a notificaciones" es un Special App Access que el usuario habilita
     manualmente desde Settings (lo abrimos con NotificationListener.openSettings()). -->
